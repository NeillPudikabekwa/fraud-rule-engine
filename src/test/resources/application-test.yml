spring:
  datasource:
    url: jdbc:h2:mem:fraud-rule-engine-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
fraud:
  rules:
    highAmountThreshold: 50000
    velocityWindowMinutes: 10
    velocityTransactionLimit: 3
    highRiskCountries: NG,RU,KP,IR
